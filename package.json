{
  "name": "@elata/elata-vsm-system-4",
  "private": true,
  "workspaces": [
    "shared-types",
    "scraper",
    "web"
  ],
  "scripts": {
    "build": "npm run build --workspace @elata/shared-types && npm run build --workspaces --if-present",
    "dev": "npm run dev --workspace web",
    "scrape": "npm run start --workspace scraper",
    "serve": "npm run serve --workspace scraper"
  }
}
